databaseChangeLog:
  - changeSet:
      id: postgres-001-create-deployment-marker
      author: adbsidecar
      changes:
        - createTable:
            tableName: deployment_marker
            columns:
              - column:
                  name: id
                  type: INTEGER
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: created_at
                  type: TIMESTAMP
                  defaultValueComputed: CURRENT_TIMESTAMP
                  constraints:
                    nullable: false
              - column:
                  name: source
                  type: VARCHAR(64)
                  defaultValue: liquibase
