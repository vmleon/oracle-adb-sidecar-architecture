changelogFile=db.changelog-master.yaml
url=jdbc:oracle:thin:@//{{ databases_private_ip }}:1521/FREEPDB1
username=SYSTEM
password={{ oracle_db_password }}
liquibase.hub.mode=off
# Selects the prod path: blockchain `transactions` table (oracle-002-create-transactions-prod).
# Override with `--contexts=dev` to get a plain `transactions` table for fast iteration
# (no NO DELETE 16-day floor blocking row removal).
contexts=prod
