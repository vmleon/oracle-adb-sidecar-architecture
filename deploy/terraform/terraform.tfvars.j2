# Auto-generated by `./manage.py setup`
# Live AI Hub (AI Data Gateway) — Terraform variables
# DO NOT EDIT — regenerate with: ./manage.py setup

region              = "{{ region }}"
home_region         = "{{ home_region }}"
config_file_profile = "{{ profile }}"
tenancy_ocid        = "{{ tenancy_ocid }}"
compartment_ocid    = "{{ compartment_ocid }}"
project_name        = "{{ project_name }}"

ssh_public_key       = "{{ ssh_public_key }}"
ssh_private_key_path = "{{ ssh_private_key_path }}"

adb_admin_password   = "{{ adb_admin_password }}"
oracle_db_password   = "{{ oracle_db_password }}"
postgres_db_password = "{{ postgres_db_password }}"
mongo_db_password    = "{{ mongo_db_password }}"

oci_genai_region         = "{{ genai_region }}"
oci_genai_compartment_id = "{{ genai_compartment_id }}"

# The gateway's resource principal needs a dynamic group + policy in the tenancy
# root. Set false if you lack tenancy IAM rights; an administrator then applies
# `terraform output resource_principal_statements` by hand.
create_identity_resources = {{ create_identity_resources }}
